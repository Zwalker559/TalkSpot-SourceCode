
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    function isManager() {
      // Check if the requesting user's role is a manager type.
      return get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role in ['Lead-Manager', 'Sub-Manager'];
    }

    function isLeadManager() {
      // Check if the requesting user is a Lead-Manager.
      return get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'Lead-Manager';
    }

    match /users/{userId} {
      // Allow any authenticated user to read any user's profile.
      // Add a specific 'get' rule to allow cross-document reads in functions.
      allow get: if request.auth != null;
      allow list: if request.auth != null;
      
      allow create: if request.auth.uid == userId;

      // Allow updates under specific conditions:
      // 1. The user is updating their own document.
      // 2. The requesting user is a Lead-Manager.
      // 3. The requesting user is a Sub-Manager AND the target user's role is 'User'.
      allow update: if request.auth.uid == userId ||
                    isLeadManager() ||
                    (isManager() && get(/databases/$(database)/documents/users/$(userId)).data.role == 'User');

      // Allow deletes under similar conditions:
      // 1. The requesting user is a Lead-Manager.
      // 2. The requesting user is a Sub-Manager AND the target user's role is 'User'.
      allow delete: if isLeadManager() ||
                     (isManager() && get(/databases/$(database)/documents/users/$(userId)).data.role == 'User');
    }

    match /user_lookups/{lookupId} {
      allow read: if true;
      allow create: if request.auth.uid == request.resource.data.uid;
      // Allow update if user is updating their own doc, OR if user is a manager
      allow update: if request.auth.uid == lookupId || isManager();
      // Allow delete if user is a manager
      allow delete: if isManager();
    }
    
    match /password_recovery/{userId} {
      allow read: if true;
      allow create: if request.auth.uid == userId;
      allow update: if request.auth.uid == userId;
    }

    match /requests/{requestId} {
        allow read: if request.auth.uid == resource.data.to || request.auth.uid == resource.data.from;
        allow create: if request.auth.uid == request.resource.data.from;
        allow update: if request.auth.uid == resource.data.to || request.auth.uid == resource.data.from;
        allow delete: if request.auth.uid == resource.data.to || request.auth.uid == resource.data.from;
    }

    match /conversations/{conversationId} {
      allow read, update, delete: if request.auth.uid in resource.data.participants;
      allow create: if request.auth.uid in request.resource.data.participants;
    }

    match /conversations/{conversationId}/messages/{messageId} {
       allow read, delete: if request.auth.uid in get(/databases/$(database)/documents/conversations/$(conversationId)).data.participants;
       allow create: if request.auth.uid == request.resource.data.senderId && request.auth.uid in get(/databases/$(database)/documents/conversations/$(conversationId)).data.participants;
    }

    match /Sponsorships/{sponsorshipId} {
      // Separate list permission for collection-level reads
      allow list: if isManager();
      // Keep other permissions for document-level access
      allow get, create, update, delete: if isManager();
    }
  }
}
